#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    hibernate:
#      ddl-auto: none  # ✅ H2에서 schema.sql을 실행하도록 설정 (update 대신 none 사용)
#    show-sql: true
#  sql:
#    init:
#      mode: always  # ✅ schema.sql -> data.sql 순서대로 실행되도록 설정
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#      settings:
#        web-allow-others: true
